services:
  - type: web
    name: two-pointer-algorithms-api
    env: docker
    plan: free
    dockerfilePath: ./Backend/Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 8080
